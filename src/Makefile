CC=cc
CFLAGS=-Wall
SOURCES=app.c
EXECUTABLE=app
BUILDDIR=bin

all: $(EXECUTABLE)

$(EXECUTABLE): $(BUILDDIR) $(SOURCES)
	$(CC) $(CFLAGS) $(SOURCES) -lpthread -o $(BUILDDIR)/$(EXECUTABLE)

$(BUILDDIR): $(SOURCES)
	mkdir -p $(BUILDDIR)

clean:
	rm -rf $(BUILDDIR)

